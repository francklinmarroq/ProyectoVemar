<UserControl
    x:Class="RemeasuresList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Vemar.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Width="317"
                Padding="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Center" />
        </Grid>
        <ListView
            Grid.Row="1"
            Height="410"
            Margin="0,10,0,0"
            VerticalAlignment="Top"
            ItemsSource="{Binding Remedidas}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Id}" Header="ID" />
                    <GridViewColumn DisplayMemberBinding="{Binding Fecha}" Header="FECHA" />
                    <GridViewColumn DisplayMemberBinding="{Binding ClienteId}" Header="CLIENTE" />
                    <GridViewColumn DisplayMemberBinding="{Binding Representante}" Header="REPRESENTANTE" />
                    <GridViewColumn DisplayMemberBinding="{Binding Ubicacion}" Header="UBICACION" />
                    <GridViewColumn DisplayMemberBinding="{Binding ClaveSure}" Header="CLAVE SURE" />
                    <GridViewColumn DisplayMemberBinding="{Binding Matricula}" Header="MATRÍCULA" />
                    <GridViewColumn DisplayMemberBinding="{Binding Cam}" Header="CAM" />
                    <GridViewColumn DisplayMemberBinding="{Binding Objeto}" Header="OBJETO" />
                    <GridViewColumn DisplayMemberBinding="{Binding Precio}" Header="PRECIO" />
                    <GridViewColumn DisplayMemberBinding="{Binding ExpedienteEntregado}" Header="EXPEDIENTE ENTREGADO?" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
