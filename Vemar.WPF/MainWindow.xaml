<Window
    x:Class="MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Vemar.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="1080"
    Height="720"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Border
        Background="{StaticResource SecondaryBrush}"
        CornerRadius="16"
        SnapsToDevicePixels="True">
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <local:AppBar
                x:Name="appBar"
                Grid.Row="0"
                Padding="5"
                DataContext="{Binding Me}" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width=".3*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <local:SideBar Grid.Column="0" DataContext="{Binding Navigator}" />
                <StackPanel Grid.Column="1">
                    <TextBlock
                        Margin="15,15,0,0"
                        FontWeight="Bold"
                        Foreground="{StaticResource TextLightBrush}"
                        Text="{Binding Navigator.CurrentViewModel}" />
                    <ContentControl Margin="15" Content="{Binding Navigator.CurrentViewModel}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>

</Window>
