<Window
    x:Class="DetalleTramiteWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="420"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <StackPanel Margin="15" Width="390">
        <GroupBox Header="Datos del Trámite" Margin="0,0,0,10">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Descripción:" VerticalAlignment="Top" Margin="0,6,0,0" />
                <TextBox
                    Grid.Row="0"
                    Grid.Column="1"
                    Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    Height="80"
                    Margin="0,4"
                    VerticalScrollBarVisibility="Auto" />
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button
                Command="{Binding GuardarCommand}"
                Content="Guardar"
                Width="90"
                Margin="0,0,8,0"
                Padding="4" />
            <Button Content="Cancelar" Width="90" Padding="4" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
