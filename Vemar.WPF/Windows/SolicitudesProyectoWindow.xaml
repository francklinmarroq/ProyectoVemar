<Window x:Class="SolicitudesProyectoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="{Binding TituloProyecto}" WindowStyle="ThreeDBorderWindow" ResizeMode="CanResize"
        Height="600" Width="920" WindowStartupLocation="CenterOwner"
        Background="#F1F5F9">
    <Window.Resources>
        <Style x:Key="DgHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1E3A8A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#1E40AF"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                            <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Width="6" Cursor="SizeWE"><Thumb.Template><ControlTemplate TargetType="Thumb"><Border Background="Transparent"><Rectangle Width="1" HorizontalAlignment="Right" Fill="#40FFFFFF"/></Border></ControlTemplate></Thumb.Template></Thumb>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DgCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DgRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#F0F4F8" BorderThickness="0,0,0,1">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" Grid.Column="1"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F9FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Badge Efectuado -->
        <Style x:Key="BadgeEfectuado" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="36" Height="36" CornerRadius="8" Background="#EFF6FF">
                    <iconPacks:PackIconPhosphorIcons Kind="FileArrowUp" Width="18" Height="18" Foreground="#1D4ED8"/>
                </Border>
                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TituloProyecto}" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="{Binding ItemsView.Count, StringFormat='Total: {0} solicitudes emitidas'}" FontSize="12" Foreground="#3B82F6"/>
                </StackPanel>
            </StackPanel>
            <!-- Valor del proyecto -->
            <Border Grid.Column="1" Background="#1E3A8A" CornerRadius="8" Padding="14,8">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconPhosphorIcons Kind="Buildings" Width="14" Height="14" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Foreground="White" FontSize="12" Margin="6,0,0,0" VerticalAlignment="Center">
                        <Run Text="Valor proyecto: "/>
                        <Run Text="{Binding ValorProyecto, Mode=OneWay, StringFormat='L {0:N2}'}" FontWeight="Bold"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Tarjetas resumen -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total solicitado -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="14,10">
                <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.06" Color="#000"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="TOTAL SOLICITADO" FontSize="10" FontWeight="SemiBold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding TotalSolicitado, StringFormat='L {0:N2}'}" FontSize="18" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Efectuado -->
            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="14,10">
                <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.06" Color="#000"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="COBRADO / EFECTUADO" FontSize="10" FontWeight="SemiBold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding TotalEfectuado, StringFormat='L {0:N2}'}" FontSize="18" FontWeight="Bold" Foreground="#16A34A" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Pendiente de cobro -->
            <Border Grid.Column="4" Background="White" CornerRadius="10" Padding="14,10">
                <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.06" Color="#000"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="PENDIENTE DE COBRO" FontSize="10" FontWeight="SemiBold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding TotalPendiente, StringFormat='L {0:N2}'}" FontSize="18" FontWeight="Bold" Foreground="#D97706" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Saldo restante -->
            <Border Grid.Column="6" Background="White" CornerRadius="10" Padding="14,10">
                <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.06" Color="#000"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="SALDO SIN SOLICITAR" FontSize="10" FontWeight="SemiBold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding SaldoRestante, StringFormat='L {0:N2}'}" FontSize="18" FontWeight="Bold" Foreground="#7C3AED" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Tabla -->
        <Border Grid.Row="4" CornerRadius="12" Background="White" ClipToBounds="True">
            <Border.Effect><DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.06" Color="#000"/></Border.Effect>
            <DataGrid ItemsSource="{Binding ItemsView}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0"
                      SelectionMode="Single" RowStyle="{StaticResource DgRowStyle}"
                      CellStyle="{StaticResource DgCellStyle}" ColumnHeaderStyle="{StaticResource DgHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="N°" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="DESCRIPCIÓN" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    <DataGridTextColumn Header="MONTO" Binding="{Binding Monto, StringFormat='L {0:N2}'}" Width="120"/>
                    <DataGridTextColumn Header="FORMA DE PAGO" Binding="{Binding FormaPago}" Width="130"/>
                    <!-- Estado con badge de color -->
                    <DataGridTemplateColumn Header="ESTADO" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EsEfectuado}" Value="True">
                                                    <Setter Property="Background" Value="#DCFCE7"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding EsEfectuado}" Value="False">
                                                    <Setter Property="Background" Value="#FEF9C3"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EsEfectuado}" Value="True">
                                                        <Setter Property="Text" Value="Efectuado"/>
                                                        <Setter Property="Foreground" Value="#15803D"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EsEfectuado}" Value="False">
                                                        <Setter Property="Text" Value="Pendiente"/>
                                                        <Setter Property="Foreground" Value="#A16207"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- Acciones -->
                    <DataGridTemplateColumn Header="ACCIONES" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <!-- Marcar / desmarcar efectuado -->
                                    <Button Command="{Binding DataContext.MarcarEfectuadoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" Cursor="Hand" Width="32" Height="32" Margin="2,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bg" CornerRadius="7">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding EsEfectuado}" Value="True">
                                                                    <Setter Property="Background" Value="#FEF9C3"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding EsEfectuado}" Value="False">
                                                                    <Setter Property="Background" Value="#DCFCE7"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <iconPacks:PackIconPhosphorIcons Width="15" Height="15"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <iconPacks:PackIconPhosphorIcons.Style>
                                                            <Style TargetType="iconPacks:PackIconPhosphorIcons">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding EsEfectuado}" Value="True">
                                                                        <Setter Property="Kind" Value="X"/>
                                                                        <Setter Property="Foreground" Value="#A16207"/>
                                                                        <Setter Property="ToolTip" Value="Marcar como Pendiente"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding EsEfectuado}" Value="False">
                                                                        <Setter Property="Kind" Value="Check"/>
                                                                        <Setter Property="Foreground" Value="#15803D"/>
                                                                        <Setter Property="ToolTip" Value="Marcar como Efectuado"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </iconPacks:PackIconPhosphorIcons.Style>
                                                    </iconPacks:PackIconPhosphorIcons>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <!-- Ver PDF -->
                                    <Button ToolTip="Ver PDF de solicitud"
                                            Command="{Binding DataContext.VerPdfCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" Cursor="Hand" Width="32" Height="32" Margin="2,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bg" Background="#EFF6FF" CornerRadius="7">
                                                    <iconPacks:PackIconPhosphorIcons Kind="FilePdf" Width="15" Height="15" Foreground="#1D4ED8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bg" Property="Background" Value="#DBEAFE"/></Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <!-- Eliminar -->
                                    <Button ToolTip="Eliminar"
                                            Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" Cursor="Hand" Width="32" Height="32" Margin="2,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bg" Background="#FEF2F2" CornerRadius="7">
                                                    <iconPacks:PackIconPhosphorIcons Kind="Trash" Width="15" Height="15" Foreground="#EF4444" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bg" Property="Background" Value="#FEE2E2"/></Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
