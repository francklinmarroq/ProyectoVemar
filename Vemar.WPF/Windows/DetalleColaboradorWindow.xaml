<Window
    x:Class="DetalleColaboradorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:b="clr-namespace:Vemar.WPF"
    Width="420"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <StackPanel Margin="15" Width="390">
        <GroupBox Header="Datos del Colaborador" Margin="0,0,0,10">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="DNI:" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Dni, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" b:DniTextBoxBehavior.IsEnabled="True"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Nombre:" VerticalAlignment="Center" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />

                <Label Grid.Row="2" Grid.Column="0" Content="Teléfono:" VerticalAlignment="Center" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" Margin="0,4"
                         b:PhoneTextBoxBehavior.IsEnabled="True"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Fecha Nac.:" VerticalAlignment="Center" />
                <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding FechaNacimiento}" Margin="0,4" />

                <Label Grid.Row="4" Grid.Column="0" Content="Domicilio:" VerticalAlignment="Center" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Domicilio, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />

                <Label Grid.Row="5" Grid.Column="0" Content="Email:" VerticalAlignment="Center" />
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button
                Command="{Binding GuardarCommand}"
                Content="Guardar"
                Width="90"
                Margin="0,0,8,0"
                Padding="4" />
            <Button Content="Cancelar" Width="90" Padding="4" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
