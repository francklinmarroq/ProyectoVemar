<Window
    x:Class="DetalleProyectoWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:b="clr-namespace:Vemar.WPF"
    Width="420"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <StackPanel Margin="15" Width="390">
        <GroupBox Header="Datos del Proyecto" Margin="0,0,0,10">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Nombre:" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />

                <Label Grid.Row="1" Grid.Column="0" Content="Ubicación:" VerticalAlignment="Center" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Ubicacion, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />

                <Label Grid.Row="2" Grid.Column="0" Content="Matrícula:" VerticalAlignment="Center" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Matricula, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" MaxLength="6"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Clave Sure:" VerticalAlignment="Center" />
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ClaveSure, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" b:DniTextBoxBehavior.IsEnabled="True"/>

                <Label Grid.Row="4" Grid.Column="0" Content="Área (m²):" VerticalAlignment="Center" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Area, UpdateSourceTrigger=PropertyChanged}" Margin="0,4" />
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button
                Command="{Binding GuardarCommand}"
                Content="Guardar"
                Width="90"
                Margin="0,0,8,0"
                Padding="4" />
            <Button Content="Cancelar" Width="90" Padding="4" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
