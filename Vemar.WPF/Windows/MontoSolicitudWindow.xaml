<Window x:Class="MontoSolicitudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Solicitud de Pago"
        Width="420" SizeToContent="Height"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC">
    <Border Margin="20" Background="White" CornerRadius="12" Padding="24,20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.06" Color="#000"/>
        </Border.Effect>
        <StackPanel>
            <!-- Título -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Border Width="38" Height="38" CornerRadius="9" Background="#EFF6FF">
                    <iconPacks:PackIconPhosphorIcons Kind="FileArrowUp" Width="20" Height="20" Foreground="#3B82F6"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Solicitud de Pago" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Ingrese el monto a solicitar" FontSize="11" Foreground="#64748B"/>
                </StackPanel>
            </StackPanel>

            <!-- Saldo pendiente -->
            <Border Background="#F0F9FF" CornerRadius="8" Padding="14,10" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Saldo pendiente total" FontSize="12" Foreground="#475569" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtSaldoPendiente" Grid.Column="1"
                               FontSize="15" FontWeight="Bold" Foreground="#1E40AF" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Campo monto -->
            <TextBlock Text="MONTO A SOLICITAR (L)" FontSize="11" FontWeight="SemiBold"
                       Foreground="#64748B" Margin="0,0,0,6"/>
            <Border BorderBrush="#3B82F6" BorderThickness="1.5" CornerRadius="8" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="#EFF6FF" CornerRadius="7,0,0,7" Padding="10,0">
                        <TextBlock Text="L" FontSize="14" FontWeight="Bold" Foreground="#3B82F6"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBox x:Name="TxtMonto" Grid.Column="1"
                             Height="40" Padding="10,0" BorderThickness="0"
                             VerticalContentAlignment="Center" FontSize="14"
                             Foreground="#0F172A" Background="Transparent"
                             KeyDown="TxtMonto_KeyDown"/>
                </Grid>
            </Border>

            <!-- Saldo resultante -->
            <Border x:Name="PanelResultante" Background="#F0FDF4" CornerRadius="8" Padding="14,10" Margin="0,12,0,0"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Saldo restante tras este pago" FontSize="12" Foreground="#475569" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtSaldoResultante" Grid.Column="1"
                               FontSize="14" FontWeight="Bold" Foreground="#16A34A" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Botones -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="BtnCancelar" Content="Cancelar" Cursor="Hand" Margin="0,0,8,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="18,9">
                                <TextBlock Text="{TemplateBinding Content}" FontSize="13" Foreground="#374151"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#F3F4F6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="BtnAceptar" Content="Generar PDF" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="#3B82F6" CornerRadius="8" Padding="18,9">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconPhosphorIcons Kind="FilePdf" Width="14" Height="14" Foreground="White" VerticalAlignment="Center"/>
                                    <TextBlock Text="{TemplateBinding Content}" FontSize="13" FontWeight="SemiBold" Foreground="White" Margin="6,0,0,0"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#2563EB"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
