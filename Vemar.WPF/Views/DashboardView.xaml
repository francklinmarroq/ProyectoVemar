<UserControl
    x:Class="DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    Background="#F1F5F9"
    mc:Ignorable="d">
    <UserControl.Resources>
        <!-- DataGrid Styles -->
        <Style x:Key="DgHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DgCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DgRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#F0F4F8" BorderThickness="0,0,0,1">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" Grid.Column="1"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F9FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36,32,36,36">

            <!-- ── Header ── -->
            <Grid Margin="0,0,0,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Panel de Control" FontSize="28" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="{Binding FechaHoy}" FontSize="13" Foreground="#3B82F6" Margin="0,6,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="14,10" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.08" Color="#000"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconPhosphorIcons Kind="Buildings" Width="18" Height="18" Foreground="#3B82F6" VerticalAlignment="Center"/>
                        <TextBlock Text="Constructora Vemar" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ── Tarjetas de estadísticas ── -->
            <TextBlock Text="RESUMEN GENERAL" FontSize="11" FontWeight="Bold" Foreground="#2563EB" Margin="0,0,0,14"/>

            <UniformGrid Rows="2" Columns="3" Margin="0,0,0,32">

                <!-- Clientes -->
                <Border Margin="0,0,10,10" CornerRadius="12" Background="White">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.07" Color="#000"/>
                    </Border.Effect>
                    <Grid Margin="20,18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Width="40" Height="40" CornerRadius="10" Background="#EFF6FF" HorizontalAlignment="Left">
                            <iconPacks:PackIconPhosphorIcons Kind="Users" Width="20" Height="20" Foreground="#3B82F6"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding TotalClientes}" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,10,0,0"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="Clientes" FontSize="13" Foreground="#3B82F6"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Remedidas -->
                <Border Margin="5,0,5,10" CornerRadius="12" Background="White">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.07" Color="#000"/>
                    </Border.Effect>
                    <Grid Margin="20,18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Width="40" Height="40" CornerRadius="10" Background="#EFF6FF" HorizontalAlignment="Left">
                            <iconPacks:PackIconPhosphorIcons Kind="Ruler" Width="20" Height="20" Foreground="#3B82F6"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding TotalRemedidas}" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,10,0,0"/>
                        <TextBlock Grid.Row="2" Text="Remedidas" FontSize="13" Foreground="#3B82F6" Margin="0,4,0,0"/>
                    </Grid>
                </Border>

                <!-- Proyectos -->
                <Border Margin="10,0,0,10" CornerRadius="12" Background="White">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.07" Color="#000"/>
                    </Border.Effect>
                    <Grid Margin="20,18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Width="40" Height="40" CornerRadius="10" Background="#EFF6FF" HorizontalAlignment="Left">
                            <iconPacks:PackIconPhosphorIcons Kind="Buildings" Width="20" Height="20" Foreground="#3B82F6"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding TotalProyectos}" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,10,0,0"/>
                        <TextBlock Grid.Row="2" Text="Proyectos" FontSize="13" Foreground="#3B82F6" Margin="0,4,0,0"/>
                    </Grid>
                </Border>

                <!-- Colaboradores -->
                <Border Margin="0,0,10,0" CornerRadius="12" Background="White">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.07" Color="#000"/>
                    </Border.Effect>
                    <Grid Margin="20,18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Width="40" Height="40" CornerRadius="10" Background="#EFF6FF" HorizontalAlignment="Left">
                            <iconPacks:PackIconPhosphorIcons Kind="UserCircle" Width="20" Height="20" Foreground="#3B82F6"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding TotalColaboradores}" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,10,0,0"/>
                        <TextBlock Grid.Row="2" Text="Colaboradores" FontSize="13" Foreground="#3B82F6" Margin="0,4,0,0"/>
                    </Grid>
                </Border>

                <!-- Contratistas -->
                <Border Margin="5,0,5,0" CornerRadius="12" Background="White">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.07" Color="#000"/>
                    </Border.Effect>
                    <Grid Margin="20,18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Width="40" Height="40" CornerRadius="10" Background="#EFF6FF" HorizontalAlignment="Left">
                            <iconPacks:PackIconPhosphorIcons Kind="HardHat" Width="20" Height="20" Foreground="#3B82F6"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding TotalContratistas}" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,10,0,0"/>
                        <TextBlock Grid.Row="2" Text="Contratistas" FontSize="13" Foreground="#3B82F6" Margin="0,4,0,0"/>
                    </Grid>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>

