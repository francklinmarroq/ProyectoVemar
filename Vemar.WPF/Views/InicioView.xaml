<UserControl
    x:Class="InicioView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Vemar.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel>
        <StackPanel FlowDirection="LeftToRight" Orientation="Horizontal">
            <local:RemedidasCard x:Name="remedidasCard" DataContext="{Binding RemedidasViewModel.Remedidas}" />
            <Border
                Width="350"
                Height="300"
                Margin="0,0,15,0"
                Padding="10"
                Background="{StaticResource PrimaryBrush}"
                BorderThickness="2"
                CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="3"
                        Opacity="0.1"
                        ShadowDepth="0.1" />
                </Border.Effect>
                <StackPanel>
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontWeight="bold"
                            Text="Projects" />
                        <Button
                            Grid.Column="1"
                            Width="25"
                            Height="25"
                            Background="{StaticResource PrimaryBrush}"
                            BorderThickness="0"
                            Content="&gt;" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding AbrirAgregarClienteCommand}" Content="Agregar Clientes" />
        </StackPanel>
    </StackPanel>
</UserControl>
